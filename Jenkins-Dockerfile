FROM jenkins/jenkins:lts

USER root

RUN apt-get update && \
    apt-get install -y docker.io && \
    rm -rf /var/lib/apt/lists/*

# Dynamically get host docker GID
ARG DOCKER_GID
RUN groupadd -for -g $DOCKER_GID docker \
    && usermod -aG $DOCKER_GID jenkins

USER jenkins
